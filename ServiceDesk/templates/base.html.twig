<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% block title %}ServiceDesk{% endblock %}</title>

  {# Минимальная “встроенная” стилизация, чтобы не зависеть от ассетов #}
  <style>
    :root { --bg:#0b1020; --card:#121a33; --text:#e6e9f5; --muted:#aab2d5; --line:rgba(255,255,255,.10);
            --ok:#2ecc71; --warn:#f1c40f; --bad:#e74c3c; --info:#3498db; }
    *{box-sizing:border-box} body{margin:0;font:15px/1.45 system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;background:var(--bg);color:var(--text);}
    a{color:#9ec6ff;text-decoration:none} a:hover{text-decoration:underline}
    .wrap{max-width:1080px;margin:0 auto;padding:18px}
    .card{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:16px}
    .row{display:flex;gap:14px;flex-wrap:wrap}
    .row > *{flex:1 1 auto}
    .muted{color:var(--muted)}
    .h{margin:0 0 10px;font-size:22px}
    .btn{display:inline-block;border:1px solid var(--line);background:rgba(255,255,255,.06);color:var(--text);
         padding:9px 12px;border-radius:10px;text-decoration:none;cursor:pointer}
    .btn:hover{background:rgba(255,255,255,.10);text-decoration:none}
    .btnPrimary{background:rgba(158,198,255,.18);border-color:rgba(158,198,255,.35)}
    .btnDanger{background:rgba(231,76,60,.16);border-color:rgba(231,76,60,.35)}
    .tag{display:inline-flex;gap:6px;align-items:center;border:1px solid var(--line);border-radius:999px;
         padding:3px 10px;font-size:12px;color:var(--muted)}
    .tag.ok{color:var(--ok)} .tag.warn{color:var(--warn)} .tag.bad{color:var(--bad)} .tag.info{color:var(--info)}
    .table{width:100%;border-collapse:collapse}
    .table th,.table td{border-bottom:1px solid var(--line);padding:10px 8px;text-align:left;vertical-align:top}
    .table th{color:var(--muted);font-weight:600}
    .hr{border:0;border-top:1px solid var(--line);margin:14px 0}
    .grid2{display:grid;grid-template-columns:1fr 1fr;gap:14px}
    @media (max-width: 820px){ .grid2{grid-template-columns:1fr} }
    input,select,textarea{width:100%;border:1px solid var(--line);background:rgba(0,0,0,.10);color:var(--text);
                          border-radius:10px;padding:10px 10px;outline:none}
    label{display:block;margin:0 0 6px;color:var(--muted)}
    .formRow{margin:0 0 12px}
    .error{color:var(--bad);font-size:13px;margin-top:6px}
    .help{color:var(--muted);font-size:13px;margin-top:6px}
    .top{display:flex;align-items:center;justify-content:space-between;gap:14px}
    .brand{font-weight:800;letter-spacing:.2px}
    .right{display:flex;gap:10px;align-items:center;flex-wrap:wrap;justify-content:flex-end}
  </style>

  {% block stylesheets %}{% endblock %}
</head>
<body>
  <div class="wrap">
    {% include '_partials/_nav.html.twig' %}
    {% include '_partials/_flash.html.twig' %}

    {% block body %}{% endblock %}

    <div class="hr"></div>
    <div class="muted" style="font-size:13px">
      ServiceDesk demo • Symfony + SQLite • <a href="/healthz">/healthz</a>
    </div>
  </div>

  {% block javascripts %}{% endblock %}
</body>
</html>
